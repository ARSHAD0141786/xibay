import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RequestAcceptedPage } from './request-accepted';

@NgModule({
  declarations: [
    RequestAcceptedPage,
  ],
  imports: [
    IonicPageModule.forChild(RequestAcceptedPage),
  ],
})
export class RequestAcceptedPageModule {}
